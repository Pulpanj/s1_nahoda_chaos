---
title: "Post With Code"
author: "Jaroslav Půlpán"
date: "2025-11-04"
updated: "2025-11-04"
categories: [news, code, analysis]
image: "image.jpg"

# project:
#   type: website

# metadata-files:
#   - _environment.yml
engine: julia
# execute:
#     python: d:/ajps/quarto/.venv/Scripts/python.exe
# set par.raw(numbering: "1")
# , inset: 1em, radius: 0.5em, width: 100%)

format:
    typst:
        keep-typ: true
        toc: false
        number-sections: true
        toc-depth: 4       # Optional: controls heading levels shown
        number-depth: 4
        include-in-header:
            - text: |
                #show raw: set text(font: "Cascadia Code")
                #show raw.where(block: true): set block(
                fill: rgb("#f9f9f9ff"),
                inset: 10pt,
                radius: 4pt,
                stroke: 0.3pt +  rgb("#c2c2c1ff"),
                )
                #set raw(theme: "/resources/jp.tmTheme")
                #show raw.where(block: true): code => {
                show raw.line: line => {
                    if line.number < 10 {
                    text(fill: olive,style:"italic",weight:"light")[~#line.number]
                    } else {
                    text(fill: olive,style:"italic",weight:"light")[#line.number]
                    }
                    h(1em)
                    line.body
                }
                code
                }

        code-annotations: below
        # code-block-border-left: true
        # code-block-bg: false
        # highlight-style: espresso
        # highlight-style: tango
        # code-line-numbers: true
    html:
        code-block-border-left: "#0b4edeff" 
        #"#2561f7ff"
        code-block-bg: "#f9f2f2ff"
        highlight-style: jptango.theme
        # highlight-style: tango
        code-line-numbers: true
        code-overflow: wrap
        code-annotations: hover
        code-fold: true
        code-tools: true
css: /styles.css        
---

This is a post with executable code.


```{julia}
using PythonCall
# @pyexec """print(1)"""
# PythonCall.pyconfig.python = "d:/ajps/quarto/.venv/Scripts/python.exe"
# using Plots
# plot(sin, 0, 2π, label="Julia sine wave")


# using PythonCall

@pyexec """
def add(a, b):
    return a + b
""" => add

add(2, 3)
```

## python print

```{python}
a=\"\"\"
lkdf;ldfsk
ldsfkdl;gk
\"\"\"

print(a)
print(11111)

import sys
print(sys.version)  
print(sys.executable)
```

```{julia}
#=
kadskf lsd sdls adfkd'f;kasdfkas;
af;sdf;'dfkgldfkd;lfgks'
=#
using Pkg
Pkg.envdir() # <1>
Pkg.status() # <2>
```


1. Take `penguins`, and then,
2. add new columns for the bill ratio and bill area.



## 🧪 xx Julia Block


### Parametric Plots

Plot function pair (x(u), y(u)). 
See @fig-parametric for an example.

```{julia}
#| label: fig-parametric
#| fig-cap: "Parametric Plots"
print(ENV)

# using Plots

# plot(sin, 
#     x->sin(2x), 
#     0, 
#     2π, 
#     leg=false, 
#     fill=(0,:lavender))
```


```{julia}
# using PythonCall
# PythonCall.pyconfig.python = "d:/ajps/quarto/.venv/Scripts/python.exe"
using Plots
plot(sin, 0, 2π, label="Julia sine wave")
```




## python print

```{python}
print(11111)

import sys
print(sys.version)  
print(sys.executable)

# help('modules package')

```


Create some data in Julia:


```{julia}
using DataFrames,DataFramesMeta, MarkdownTables, Random, Distributions
```

```{julia}
data = DataFrame(x=sin.(0:pi/6:4pi))
markdown_table(data)
```




<!-- ```{python}
$data

``` -->


### python tab
```{python}
#| tbl-cap: "Tabulka pravděpodobností 6-ky v k-tém hodu"
#| label: tbl-pk
import pandas as pd

k=list([])
pk=list([])
kmax=100
for kk in range(1,kmax+1):
    k.append(kk)
    p=(5/6)**(kk-1)/6
    pk.append(p)
# print(pk)
df=pd.DataFrame(
    zip(k,pk)
    , columns=['k', 'P(k)']
    )
df.set_index('k', inplace=True)    
df['Sum(P(k))']=df['P(k)'].cumsum()

# Define a list of indices to filter
list_to_print = list(range(1,11))+[20,30,40,50,80,100]
df[df.index.isin(list_to_print)]
```

::: {.callout-note}
Note that there are five types of callouts, including:
`note`, `warning`, `important`, `tip`, and `caution`.
:::

::: {.callout-tip}
## Tip with Title

This is an example of a callout with a title.
:::

::: {.callout-caution collapse="true"}
## Expand To Learn About Collapse

This is an example of a 'folded' caution callout that can be expanded by the user. You can use `collapse="true"` to collapse it by default or `collapse="false"` to make a collapsible callout that is expanded by default.
:::


::: {.callout-caution collapse="true"}
## Expand To Learn About Collapse

```{julia}
data = DataFrame(x=sin.(0:pi/6:4pi))
markdown_table(data)
```

:::



```{julia}
#| label: fig-plots
#| fig-cap: "Multiple Plots"
#| fig-subcap:
#|   - "Plot 1"
#|   - "Plot 2"
#| layout-ncol: 2

using Plots
display(plot(sin, x -> sin(2x), 0, 2))
display(plot(x -> sin(4x), y -> sin(5y), 0, 2))
```